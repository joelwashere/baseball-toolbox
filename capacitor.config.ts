import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.reboundstudios.bt",
  appName: "Baseball Toolbox",
  webDir: "out",
  bundledWebRuntime: false
};

export default config;
